INCLUDE_DIRECTORIES(../../inc)
INCLUDE_DIRECTORIES(netinfd)
AUX_SOURCE_DIRECTORY(. LINUX_C_API)
AUX_SOURCE_DIRECTORY(netinfd NETINFD)

ADD_LIBRARY(linux_c_api ${LINUX_C_API} ${NETINFD})
TARGET_LINK_LIBRARIES(linux_c_api curl ipcd)
